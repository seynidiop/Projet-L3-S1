@model IEnumerable<Projet_L3_S1.Models.menu>

<div class="products">
@foreach (var b in Model)
{
    <div class="product-card">
        <img src="@b.imagepath" alt="@b.nom" />

        <div class="content">
            <h3>@b.nom</h3>
            <p>@b.description</p>
            <p class="price">@b.price FCFA</p>
        </div>

        <button onclick="addToCart(
            @b.id,
            '@b.nom',
            @b.price,
            '@Url.Content(b.imagepath)',
            'burger'
        )">
            Commander
    </button>
    </div>
}
</div>
